# 清稿工具 · 后续计划

> 目标：把当前"金额单位统一"单点脚本，演进为一套**确定性清稿模块层 + AI 编排层**的清稿工具。
> 核心原则：**准确性第一**。AI 只做"结构分析与模块路由"，绝不生成/猜测数据；实际换算全部交给确定性脚本完成。

---

## 总体判断

三点计划都可行，且能自然分层：

```
AI 编排层   （方向3）table-clean skill：探查表格 → 判断列类型 → 选模块/参数 → 汇总需人工
─────────────────────────────────────────────────────────────────────────
确定性模块层 （方向1+2）通用金额单位统一器(目标/源单位可配) 及更多子任务模块：只做有依据的换算，不确定的交人工
```

- **方向1/2** 夯实底层确定性模块（"会做什么"）。
- **方向3** 用 pi skill 把模块串成"上传表格→自动清稿"的工作流（"怎么被调用"）。

---

## 〇、脚本定位升级：从“统一为万元”到“通用金额单位统一器”

**问题**：当前 `amount_unifier` 目标单位硬编码为“万元”，只能把 元/万/亿 归到万元；若某张表要统一为“元”或“亿元”就无能为力。

**结论：参数化现有脚本，而非另写。** 分两个维度看：

| 维度 | 属于 | 做法 |
|---|---|---|
| 目标单位（元/千元/万元/亿元） | 金额统一任务**内部**的参数 | **参数化**（`--target-unit`） |
| 任务类型（金额/日期/枚举/文本） | **不同任务** | 各写规则模块，**共用同一基类** `BaseColumnCleaner` |

即：**纵向参数化 + 横向模块化 + 共享基类**。不要把各种任务全塞进一个巨型脚本，也不要每个从零写。

**设计：单位体系表**
- 定义各单位对“元”的比例：`UNIT_SCALE = {元:1, 千元:1e3, 万元:1e4, 亿元:1e8}`
- 解析文本得 `(数值, 源单位)`，换算：`结果 = 数值 × UNIT_SCALE[源] / UNIT_SCALE[目标]`
- 源单位同义词归一（`万/万元/W`、`元/圆`）
- 目标单位参数 `--target-unit`，**默认“万元”**（向后兼容现有用法）
- **无单位的纯数值/公式仍保守保留**（守准确性底线，不猜源单位）

**收益**：一套代码支持 元/万/亿/千元 互转，覆盖不同报送口径；与 `BaseColumnCleaner` 契合——单位换算是“规则”，Excel 编排是“基类”。

**进度**：✅ 已实现 `--target-unit`、`UNIT_SCALE`、`unit_rules_for(target_unit)`；源单位已含 元/千元/万元/亿元；图例已随目标单位中性化；无单位的纯数值/公式仍保守保留。

---

## 一、补充更多种情况（方向1）

**可行性**：高，成本低。规则表驱动，加"明确单位"的 `TextUnitRule` 即可。

**可补清单**：
- 单位：✅ `亿元`、✅ `千元`（已实现）；`圆`、`元整`（待）
- 杂质：全角数字/逗号、前后空格与不可见字符、`¥ / ￥ / RMB` 前缀（待）
- 形态：`1,000,000.00`（带千分位+小数）、科学计数法文本（待）

**底线（不变）**：
- 只在有**明确单位标识**时换算；无单位的一律保守保留/转人工。
- 不加"凭数值大小猜单位"这类逻辑（纯数值、公式均已改为如实保留）。

---

## 二、开发更多模块（方向2）

**可行性**：高。**先抽象基类，再复制开发**，避免每个模块重写 Excel 编排。

**先抽基类** `BaseColumnCleaner`，沉淀各模块共用能力：
- 列定位、合并继承/复刻、`append`/`overwrite` 两种落地、上色、报告、`--json` 输出。

**候选子任务模块**（按场景清晰度/风险排序）：
1. 日期统一（多格式 → 标准日期）
2. 文本清洗（全/半角、去空白、去不可见字符）
3. 枚举归并（企业名/险种同义词）
4. 结构校验（序号重排、空值/重复检测、跨列合理性）

---

## 三、编写 skill 实现自动化清稿（方向3）

**可行性**：高。pi 的 skill 机制成熟支持（`SKILL.md` + `scripts/` + `references/` + `assets/`，可配 `allowed-tools`，支持 `/skill:name` 调用）。

**skill 结构**：
```
table-clean/
├── SKILL.md              # 指导AI：探查→判断列类型→选模块/参数→汇总需人工
├── scripts/              # amount_unifier.py 及未来各模块
├── references/           # 各模块用法、列语义判断指南、常见问题清单
└── assets/               # 可选模板
```

**AI 工作流**：
1. **探查表格**：读表头 + 采样前几行，判断每列类型（金额/日期/枚举/文本…）
2. **决策**：该列用哪个模块、哪些参数（`-c 列 -m 模式 -s 表 --header-row -r 行`）
3. **执行**：通过 bash 调用脚本
4. **汇总**：读回报告，把"需人工"项显式交回用户

**关键机制**：
- SKILL.md 的 `description` 决定路由（何时加载本 skill），要写清"做什么+何时用"。
- AI 只做**分析 + 路由 + 编排**，不编数据；换算全由确定性脚本完成。
- 可配 `allowed-tools` 预批准 bash 运行 scripts/ 下脚本；注意项目信任。

---

## 四、关键支撑增强（建议先落地）

| 增强 | 作用 |
|---|---|
| ✅ 脚本加 `--json` 报告输出 | 让 AI/上层**可靠解析**结果（成功/需人工/规则计数），而非解析人眼文本——skill 可靠性关键 |
| 抽 `BaseColumnCleaner` 基类 | 模块复用，避免重写 Excel 编排 |
| 极简"探查"函数/脚本 | 供 AI 读表头+采样，辅助列类型判断 |

---

## 五、落地顺序（MVP 路径）

1. **阶段A**：`amount_unifier` 加 `--json`、目标单位参数化（`--target-unit`）——✅ 已完成；抽出 `BaseColumnCleaner`——待。
2. **阶段B**：基于基类加 1–2 个新模块（建议先"日期统一"或"文本清洗"，场景清晰、风险低）。
3. **阶段C**：写 `table-clean` skill，先只串"金额列"跑通端到端，稳定后再扩展更多模块。

---

## 六、风险与底线

- **AI 可能选错列/模式** → 让 AI **保守决策**；脚本报告始终可核对；**需人工项必须显式交回，绝不静默"猜"**。
- **skill 调脚本** → 注意项目信任与 `allowed-tools` 预批准范围。
- **准确性第一** → 任何自动换算必须来自确定性规则；AI 永不生成/补全数据值。

---

## 七、下一步

阶段A 的 `--json`、`--target-unit` 已完成；图例已随目标单位中性化。剩余可选下一步：
1. 抽出 `BaseColumnCleaner` 基类（阶段A 收尾，服务方向2）。
2. 基于基类加第一个新模块（建议“文本清洗”或“日期统一”，场景清晰、风险低）。
3. 搭 `table-clean` skill 骨架，先串“金额列”跑通端到端。

推荐顺序：先 1（基类）→ 2（用一个新模块验证基类）→ 3（skill 编排）。
